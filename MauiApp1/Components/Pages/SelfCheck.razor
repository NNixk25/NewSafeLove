@page "/self-check"
<body>
<div class="page-wrapper">
    <div class="card">
        <h2 class="title">🔍 แบบประเมินความเสี่ยงส่วนตัว</h2>
    <form id="riskForm">
        <!-- คำถาม 1 -->
            <div class="question">
                <label>1. คุณเคยมีเพศสัมพันธ์โดยไม่ใช้ถุงยางอนามัยหรือไม่?</label>
                <select name="q1">
                    <option value="0">ไม่เคย</option>
                    <option value="1">บางครั้ง</option>
                    <option value="2">บ่อยครั้ง</option>
                </select>
            </div>

        <!-- คำถาม 2 -->
            <div class="question">
                <label>2. มีคู่นอนหลายคนในช่วง 6 เดือนที่ผ่านมา?</label>
                <select name="q2">
                    <option value="0">ไม่มี</option>
                    <option value="1">1-2 คน</option>
                    <option value="2">มากกว่า 2 คน</option>
                </select>
            </div>


        <!-- คำถาม 3 -->
            <div class="question">
                <label>3. คุณหรือคู่ของคุณเคยติดโรคติดต่อทางเพศหรือไม่?</label>
                <select name="q3">
                    <option value="0">ไม่เคย</option>
                    <option value="2">เคย</option>
                </select>
            </div>

        <!-- คำถาม 4 -->
            <div class="question">
                <label>4. มีอาการผิดปกติ เช่น แผล ตกขาวผิดปกติ คัน หรือปัสสาวะแสบ?</label>
                <select name="q4">
                    <option value="0">ไม่มี</option>
                    <option value="2">มี</option>
                </select>
            </div>

        <!-- คำถาม 5 -->
            <div class="question">
                <label>5. เคยใช้เข็มฉีดยาร่วมกับผู้อื่นหรือไม่?</label>
                <select name="q5">
                    <option value="0">ไม่เคย</option>
                    <option value="2">เคย</option>
                </select>
            </div>

            <button type="button" onclick="calculateRisk()" class="btn-submit">ประเมินความเสี่ยง</button>
    </form>

        <div id="result" class="result"></div>


        <a href="/"><button class="btn-back">กลับหน้าหลัก</button></a>
    </div>
</div>

<script>
    function calculateRisk() {
      const form = document.getElementById("riskForm");
      const selects = form.querySelectorAll("select");
      let totalScore = 0;

      selects.forEach(select => {
        totalScore += parseInt(select.value);
      });

      const result = document.getElementById("result");

      if (totalScore <= 2) {
        result.textContent = "✅ คุณมีความเสี่ยงต่ำ โปรดรักษาสุขภาพและป้องกันเสมอ";
        result.style.color = "#28a745";
      } else if (totalScore <= 5) {
        result.textContent = "⚠️ คุณมีความเสี่ยงปานกลาง แนะนำให้พบแพทย์เพื่อตรวจเพิ่มเติม";
        result.style.color = "#ff9800";
      } else {
        result.textContent = "🚨 คุณมีความเสี่ยงสูง ควรเข้ารับการตรวจโดยแพทย์โดยเร็วที่สุด";
        result.style.color = "#e53935";
      }
    }
</script>
</body>



<style>


    .page-wrapper {
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 30px;
    }

    .card {
        background: #fff;
        border-radius: 20px;
        padding: 25px;
        max-width: 550px;
        width: 100%;
        box-shadow: 0 8px 20px rgba(0,0,0,0.05);
        transition: transform 0.2s ease;
    }

        .card:hover {
            transform: translateY(-3px);
        }

    .title {
        text-align: center;
        margin-bottom: 20px;
        color: #d63384; /* ?????????????? */
    }

    .question {
        margin-bottom: 18px;
    }

    label {
        display: block;
        margin-bottom: 6px;
        color: #444;
        font-weight: 500;
    }

    select {
        width: 100%;
        padding: 10px;
        border: 1px solid #f8c6d8;
        border-radius: 12px;
        background: #fff0f6;
        font-size: 15px;
        color: #555;
        transition: 0.2s;
    }

        select:focus {
            outline: none;
            border-color: #e87ca6;
            background: #ffe9f3;
        }

    .btn-submit {
        width: 100%;
        padding: 12px;
        background: #ff99cc;
        border: none;
        border-radius: 12px;
        color: #fff;
        font-weight: bold;
        font-size: 16px;
        cursor: pointer;
        transition: 0.3s;
    }

        .btn-submit:hover {
            background: #ff80bf;
        }

    .btn-back {
        margin-top: 15px;
        width: 100%;
        padding: 12px;
        background: #d63384;
        border: none;
        border-radius: 12px;
        color: #fff;
        font-weight: bold;
        font-size: 16px;
        cursor: pointer;
        transition: 0.3s;
    }

        .btn-back:hover {
            background: #c2185b;
        }

    .result {
        margin-top: 20px;
        font-size: 1.1em;
        font-weight: bold;
        text-align: center;
    }

    .btn-submit {
        display: inline-block;
        margin-top: 10px;
        padding: 12px 25px;
        background: #ff99cc;
        border: none;
        border-radius: 12px;
        color: #fff !important;
        font-weight: bold;
        font-size: 16px;
        cursor: pointer;
        transition: 0.3s;
        text-decoration: none;
    }

</style>